# Template for local secrets.
#
# SETUP:
#   1. Copy this file:  cp .env.example .env
#   2. Fill in your real keys in .env
#   3. .env is gitignored, so your keys never reach GitHub.
#
# WXT/Vite bakes these values into the extension bundle at build time.

# OpenAI API key — used for the chat assistant (https://platform.openai.com/api-keys)
WXT_OPENAI_API_KEY=your-openai-api-key-here

# Google Sheets API key — used to read spreadsheet values (Google Cloud Console)
WXT_GOOGLE_SHEETS_API_KEY=your-google-sheets-api-key-here
